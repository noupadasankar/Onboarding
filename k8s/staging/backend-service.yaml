apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: optiagent-staging
  labels:
    app: backend
    app.kubernetes.io/name: optiagent
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: backend