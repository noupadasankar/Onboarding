$kind: http-request
name: Upload Document
method: POST
url: '{{aiServiceUrl}}/api/v1/documents/upload'
order: 1000
headers:
  - key: X-Internal-Token
    value: '{{internalToken}}'
body:
  type: formdata
  content:
    - key: file
      type: file
      src: ''
