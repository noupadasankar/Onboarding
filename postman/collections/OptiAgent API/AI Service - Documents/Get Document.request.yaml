$kind: http-request
name: Get Document
method: GET
url: '{{aiServiceUrl}}/api/v1/documents/:document_id'
order: 3000
headers:
  - key: X-Internal-Token
    value: '{{internalToken}}'
pathVariables:
  - key: document_id
    value: '{{aiDocumentId}}'
