$kind: http-request
name: Get Conversation by ID
method: GET
url: '{{backendUrl}}/api/v1/conversations/:id'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{conversationId}}'
