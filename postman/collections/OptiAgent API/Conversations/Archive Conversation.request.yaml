$kind: http-request
name: Archive Conversation
method: DELETE
url: '{{backendUrl}}/api/v1/conversations/:id'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{conversationId}}'
