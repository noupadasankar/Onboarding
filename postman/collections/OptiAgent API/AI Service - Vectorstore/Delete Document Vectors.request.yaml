$kind: http-request
name: Delete Document Vectors
method: DELETE
url: '{{aiServiceUrl}}/api/v1/documents/:document_id/vectors'
order: 3000
headers:
  - key: X-Internal-Token
    value: '{{internalToken}}'
pathVariables:
  - key: document_id
    value: '{{aiDocumentId}}'
