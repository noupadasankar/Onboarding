$kind: http-request
name: Get Department by ID
method: GET
url: '{{backendUrl}}/api/v1/departments/:id'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{departmentId}}'
