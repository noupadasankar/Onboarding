$kind: http-request
name: Logout
method: POST
url: '{{backendUrl}}/api/v1/auth/logout'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
