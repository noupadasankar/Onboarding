$kind: http-request
name: Deactivate User
method: DELETE
url: '{{backendUrl}}/api/v1/users/:id'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{userId}}'
