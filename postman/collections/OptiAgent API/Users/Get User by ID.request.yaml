$kind: http-request
name: Get User by ID
method: GET
url: '{{backendUrl}}/api/v1/users/:id'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{userId}}'
