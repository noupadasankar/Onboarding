$kind: http-request
name: Mark Notification Read
method: PATCH
url: '{{backendUrl}}/api/v1/notifications/:id/read'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{notificationId}}'
