$kind: http-request
name: Delete Document
method: DELETE
url: '{{backendUrl}}/api/v1/documents/:id'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{documentId}}'
