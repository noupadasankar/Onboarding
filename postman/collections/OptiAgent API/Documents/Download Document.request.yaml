$kind: http-request
name: Download Document
method: GET
url: '{{backendUrl}}/api/v1/documents/:id/download'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '{{documentId}}'
